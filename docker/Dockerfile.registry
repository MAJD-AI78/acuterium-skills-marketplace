FROM python:3.12-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY src/registry/ ./registry/
COPY registry/skills.json ./data/

CMD ["uvicorn", "registry.app:app", "--host", "0.0.0.0", "--port", "8000"]
